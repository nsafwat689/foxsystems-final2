node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/
*.tsbuildinfo

# Build output — Vercel rebuilds from source, a committed copy only goes stale.
dist/

# Source captures live outside the repo; the web-sized screens in
# client/public/showcase are what the site actually serves.
*.rar
